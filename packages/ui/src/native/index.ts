/**
 * NativeWind primitives. Kept small in Phase 0; the mobile app uses the
 * tokens directly. Add components here only after the same shape exists in
 * `src/web` so behaviour stays in sync.
 */
export {};
